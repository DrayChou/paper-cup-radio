{
  "name": "paper-cup-radio",
  "version": "0.1.1",
  "private": true,
  "description": "Paper Cup Radio, a local multi-client phone-to-desktop input relay.",
  "main": "dist/server.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "start": "npm run build && node dist/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.10",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.6",
    "typescript": "^5.8.3"
  }
}
